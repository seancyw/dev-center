<UserControl x:Class="SpriteLightRock.SprEditorForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpriteLightRock"
             mc:Ignorable="d" 
             d:DesignHeight="447" d:DesignWidth="800" xmlns:my="clr-namespace:SpriteLightRock.SystemEx">
    <UserControl.Resources>
        <local:BoolToVisibilityConverter x:Key="boolToVis" />
        <Style TargetType="{x:Type TextBlock}" 
           x:Key="GridBlockStyle">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Visibility" 
              Value="{Binding Path=IsSelected, 
              RelativeSource={RelativeSource FindAncestor, 
                        AncestorType={x:Type ListViewItem}},
              Converter={StaticResource boolToVis}, 
                         ConverterParameter=False}" />
        </Style>
        <Style TargetType="{x:Type FrameworkElement}" 
           x:Key="GridEditStyle">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Visibility" 
              Value="{Binding Path=IsSelected, 
              RelativeSource={RelativeSource FindAncestor, 
                        AncestorType={x:Type ListViewItem}},
              Converter={StaticResource boolToVis}, 
                         ConverterParameter=True}" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <ListView x:Name="lvViewModules" Margin="0,0,545,0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="50">
                        <GridViewColumnHeader Content="Id" />
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <TextBlock Text="{Binding Path=Id}" Style="{StaticResource GridBlockStyle}"/>
                                    <TextBox Text="{Binding Path=Id}" Style="{StaticResource GridEditStyle}"/>
                                </Grid>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="50" Header="X" DisplayMemberBinding="{Binding X}"/>
                    <GridViewColumn Width="50" Header="Y" DisplayMemberBinding="{Binding Y}"/>
                    <GridViewColumn Width="50" Header="Width" DisplayMemberBinding="{Binding Width}"/>
                    <GridViewColumn Width="50" Header="Height" DisplayMemberBinding="{Binding Height}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <my:CanvasEx Height="447" HorizontalAlignment="Left" Margin="261,0,0,0" x:Name="canvasEx1" VerticalAlignment="Top" Width="539" />
    </Grid>
</UserControl>
